import java.util.ArrayList;

public class Utils {
    public static Alternation parseAlternation(String pattern, int start, int end){
        ArrayList<RegexToken> list = new ArrayList<>();

        for(int i = start; i<=end; i++){
            if(pattern.charAt(i) == '|'){

            }
        }
    }
}
